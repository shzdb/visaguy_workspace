# Visa Tracking Implementation State

## Ground facts

- Workspace: `/home/shzd/Projects/workspaces/visaguy_workspace`.
- Feature: `FEAT-001` in `features/ongoing/visa-tracking/`.
- Executor: Kimi Code CLI (`kimi -p`).
- Application code does not belong in this workspace.
- Remote Frappe bench: `/home/shahzad/bench` on `erpcode.tridz.in:2257`.
- New frontend repository target: `/home/shzd/Projects/tridz/visa_tracker`.
- TASK-001, TASK-003, TASK-004, TASK-005, TASK-006, TASK-007, TASK-008, and TASK-009 are completed (runtime gates deferred); TASK-002 is in progress (runtime-blocked); TASK-010 is in progress.
- TASK-001 through TASK-010 and accepted ADR-003 through ADR-005 are materialized in the workspace.
- The current workspace checkout has pre-existing documentation edits in `README.md` and `features/planned/visa-tracking/README.md`; preserve them.
- Application changes are authorized only by the corresponding in-progress/ready task contract. No push or deployment is authorized.

## Phase table

| # | Phase | Output | Status |
|---|---|---|---|
| 1 | Readiness reconnaissance | `01-readiness-recon.md` | completed |
| 2 | Orchestrator triage | `02-triage.md` | completed |
| 3 | Ready-task and decision reconciliation | `03-planning-repair.md` | completed |
| 4a | TASK-001 reconnaissance and partial setup | `04a-task-001-recon-and-setup.md` | completed |
| 4b | TASK-001 FileFlo and extractor setup | `04b-task-001-finish-setup.md` | completed |
| 5a | TASK-002 planning | `05a-task-002-planning.md` | completed |
| 5b | TASK-002 implementation | `05b-task-002-implementation.md` | runtime-blocked |
| 6 | TASK-003 through TASK-010 planning | `06-task-003-010-planning.md` | completed |
| 7a | TASK-003 OCR/MRZ implementation | `07a-task-003-implementation.md` | completed |
| 7b | TASK-005 tracking model implementation | `07b-task-005-implementation.md` | completed |
| 7c | TASK-008 frontend design implementation | `07c-task-008-implementation.md` | completed |
| 8a | TASK-004 FileFlo queued detection | `08a-task-004-implementation.md` | completed |
| 8b | TASK-006 lifecycle/status synchronization | `08b-task-006-implementation.md` | completed (runtime-deferred) |
| 8c | TASK-007 public API and security controls | `08c-task-007-implementation.md` | completed (runtime-deferred) |
| 8d | TASK-009 frontend tracking flow | `08d-task-009-implementation.md` | completed (E2E deferred) |
| 9 | End-to-end verification (TASK-010) | `10-task-010-planning.md`, `11-task-010-evidence.md` | runtime-blocked |
| 9a | TASK-002 runtime-closure gate | `09a-task-002-runtime-closure.md` | completed (unblocked, phase 09c) |
| 9b | TASK-002/003 corrective | `09b-task-002-003-corrective.md` | completed |
| 9c | TASK-003 preprocessor corrective | `09c-task-003-preprocessor-corrective.md` | completed |
| 9d | TASK-005 module/fixture corrective | `09d-task-005-corrective.md` | completed |
| 9e | TASK-005/007 corrective | `09e-task-005-007-corrective.md` | completed |
| 9h | Settings-loader + enqueue corrective (Claude Sonnet) | `09h-settings-loader-and-enqueue-corrective.md` | completed (accepted `ccbd6324`) |
| 9f | TASK-010 backend integration tests (Kimi) | `09f-task-010-integration-tests.md` | completed 22/27 (accepted `088430f4`) |
| 9i | Autoname + timeline corrective, final 5 bodies (Sonnet) | `09i-autoname-and-timeline-corrective.md` | completed (accepted `910914e`) |
| 9g | Frontend E2E harness (Claude Sonnet) | `09g-frontend-e2e-harness.md` | completed (accepted `a6a07a9a`) |
| 9j | Live wire-contract smoke (Kimi + coordinator) | `09j-live-wire-contract-smoke.md` | completed — 0 mismatches, 1 security finding |

## Decisions log

- 2026-07-21: Start with read-only readiness reconnaissance because no task exists in `tasks/ready/`; implementation cannot safely begin before that gate is repaired.
- 2026-07-21: Treat the missing ADR references as documentation drift backed by already-accepted FEAT-001 architecture, but materialize ADR-003 through ADR-005 before creating TASK-001 so feature metadata resolves to accepted records.
- 2026-07-21: Limit the planning repair to ADR-003 through ADR-005 and TASK-001. Supporting specs 02-06 and TASK-002 through TASK-010 are not required to begin preflight and will be prepared before their dependent implementation phases.
- 2026-07-21: TASK-001 assigned; moved the task to `tasks/in-progress/` and FEAT-001 to `features/ongoing/` before application-repository work.
- 2026-07-21: Current Vite 8 documentation requires Node.js 20.19+ or 22.12+ and scaffolds React TypeScript with `npm create vite@latest <target> -- --template react-ts`; TASK-001 must verify the local Node runtime before scaffolding.
- 2026-07-21: TASK-001 phase 04a source-evidenced both Lead variants and the FileFlo/PF joins, created the clean `the_visaguy` worktree, and committed the local frontend scaffold at `20b8c45` with lint/build passing.
- 2026-07-21: Phase 04b awaits a FileFlo base-branch decision (`develop`, `main`, or deployed `fix/mandatory-file`) and confirmation of the proposed `passport_extractor` app metadata.
- 2026-07-21: User confirmed the deployed FileFlo base and extractor metadata. TASK-001 completed with runtime-verified site installation, clean feature worktrees, and recorded integration evidence.
- 2026-07-21: TASK-002 assigned after schema review corrections. Automated tests are isolated to `passport-extractor-test.localhost`; active site `visaguy` is not migrated with feature schema in this task.
- 2026-07-21: TASK-002 static implementation was committed at `6746d049`, then independently reviewed and corrected at `2f25c0d5`. All static gates pass. Dedicated-site migration and tests remain blocked because the bench has no configured `root_password` key; no runtime work was redirected to `visaguy`.
- 2026-07-21: Materialized TASK-003 through TASK-010. Started the independent first wave: TASK-003 in `passport_extractor`, TASK-005 in `the_visaguy`, and TASK-008 in local `visa_tracker`; runtime Frappe gates remain deferred to the dedicated test site.
- 2026-07-21: TASK-005 committed at `f7ad8c05` with static schema/fixture gates passing. TASK-008 committed at `a21bbd50`; lint, TypeScript, and production build pass. Both moved to completed with Frappe runtime gates retained for TASK-010.
- 2026-07-21: TASK-003 committed at `a13fa3cd` and hardened at `034f1c17`; 26 pure tests and static gates pass. TASK-004 started after correcting its dependency and public-file behavior to respect TASK-002 insert invariants.
- 2026-07-21: TASK-004 executor was interrupted while repairing pure tests; its remote uncommitted changes must be inspected and preserved before resumption. Added detailed handoff prompts for every pending task under `ongoing/visa-tracking-implementation/prompts/HANDOFF.md` in dependency order.
- 2026-07-21: Takeover orchestrator resumed TASK-004 via `08a2-task-004-resume.txt`. Recovery preserved all interrupted work; defects repaired (safe session access, LocalProxy test doubles, stale child-row payload, terminology boundary). FileFlo committed at `fa1d6b38`, the_visaguy at `34cb477`; 6+18 pure tests and all static gates pass; runtime deferred (bench `root_password` key absent — re-verified). Coordinator independently reviewed the dispatcher, payload, hooks merge, subscriber, orchestrator idempotency, and import-path diffs before acceptance. TASK-006 dispatched next.
- 2026-07-21: TASK-006 implemented and committed in the_visaguy at `92223020` (`feat: add visa tracking lifecycle sync`). Lifecycle/status/lookup/reconciliation services, Customer/PF/Passport-Extraction handlers merged into hooks without replacing existing mappings, recursion-safe bidirectional PF sync, role-gated corrections, report-only reconciliation. 82 pure tests green (4 integration skips), all static gates pass; runtime deferred (`root_password` still absent — re-verified). Report at `08b-task-006-implementation.md`; TASK-006 moved to completed, and the TASK-004 file (left in `in-progress/` by the previous coordinator) also moved to completed. TASK-007 dispatched next.
- 2026-07-21: TASK-007 implemented and committed in the_visaguy at `d20cc6d4` (`feat: add secure public tracking APIs`). Three Guest POST-only whitelisted APIs (verify identity → opaque Redis session, masked status allowlist, non-enumerating logout), HMAC-versioned lookup with server-only secret, rate-limit/lockout with HMAC-only cache keys, strict CORS allowlist, append-only redacted `Visa Tracker Audit Log` DocType. 197 tests pass (10 integration skips; 2 errors are the pre-existing TASK-005 site-bound baseline), all static/security gates pass; runtime deferred (`root_password` still absent). Deviations recorded in `08c-task-007-implementation.md` (report filename, logout API per dispatch prompt, new Audit Log DocType + three settings fields). TASK-007 moved to completed; TASK-009 dispatched next.
- 2026-07-21: TASK-009 implemented and committed in local `visa_tracker` at `717c62d4` (`feat: add public visa tracking flow`). Typed API client against the TASK-007 wire contract, RHF+Zod verification form with full a11y wiring, memory-only session token, masked status/timeline views, lockout/expiry/error states, Vitest+RTL+MSW harness with 16 tests including direct privacy assertions. Lint/tsc/tests/build pass; bundle scan clean. Frontend implements the final TASK-007 wire contract (masked field names, `{status, message, effective_on, icon}` timeline, constant-200 generic failures), not the stale TASK-009 §2.1 sketch — noted for TASK-010: mid-flow session expiry is indistinguishable from generic failure under the current backend contract. E2E deferred to TASK-010. TASK-009 moved to completed.
- 2026-07-21: TASK-002 runtime-closure gate (`09a-task-002-runtime-closure.txt`) executed: bench `common_site_config.json` still has no `root_password` key (presence checked by count only). No site created, no runtime work attempted, no fallback to `visaguy`. TASK-002 remains runtime-blocked; blocker report at `09a-task-002-runtime-closure.md`.
- 2026-07-22: Takeover orchestrator verified workspace and all four repositories at their expected committed HEADs with clean trees (`passport_extractor` `034f1c17`, `fileflo` `fa1d6b38`, `the_visaguy` `d20cc6d4`, `visa_tracker` `717c62d4`) and re-confirmed the `root_password` key is absent. TASK-010 moved to `tasks/in-progress/visa-tracking/` and dispatched via `10-task-010-final-verification.txt`; runtime matrix expected blocked on the dedicated-site gate.
- 2026-07-22: TASK-010 executed. Reconciliation of TASK-002–009 passed (all SHAs/branches/trees independently verified; dependency boundaries and secret/PII scans clean; hook/guest-API wiring re-verified). Static/pure tiers re-executed green: fileflo 6/6, passport_extractor 24 pass + 2 site-bound setUpClass errors, the_visaguy 197 pass/10 skip + 2 pre-existing site-bound errors; frontend lint/tsc/16 Vitest/build/bundle-scan all pass. Dedicated-site gate blocked: `root_password` still absent (presence-check only; no site created, no `visaguy` fallback — coordinator independently confirmed the bench sites directory contains only `visaguy`, re-ran the fileflo suite, and re-ran the frontend tests before accepting). TASK-010 verdict: NOT complete, runtime-blocked; FEAT-001 is NOT complete. TASK-002 and TASK-010 remain in `tasks/in-progress/visa-tracking/`. Reports: `10-task-010-planning.md`, `11-task-010-evidence.md`. Exact next actions: (1) bench administrator configures a `root_password` key in `sites/common_site_config.json` (value never enters this workspace); (2) bench administrator installs PyMuPDF in the bench environment (TASK-003 runtime OCR prerequisite); (3) re-dispatch TASK-010 runtime phase (`09a-task-002-runtime-closure.txt` SAFE RUNTIME WORK + TASK-010 Phase B/C) on `visa-tracker-test.localhost` only; (4) obtain and record human visual-parity approval (E2E matrix item 18); (5) only then may the workspace-maintainer move TASK-010 to completed and set FEAT-001 `status: completed`.
- 2026-07-22: User (project owner) completed the bench prerequisites: `root_password` key configured (coordinator re-verified presence by count only) and PyMuPDF 1.28.0 installed (import-verified). User accepted the current frontend design language as sufficient for now with beautification deferred — recorded as the human visual-parity decision for E2E matrix item 18 (reviewer: project owner; decision: accepted provisionally, polish follow-up planned). User authorized the dedicated-site runtime phase.
- 2026-07-22: Phase 09a re-executed unblocked: `visa-tracker-test.localhost` created and migrated, worktree-first install verified, DocType schema/permissions runtime-verified (47 fields, exact statuses, permlevel-1 raw fields, no Guest rules). Full suite 61 tests: 38 pass, 16 errors, 7 failures, reproducible. Genuine controller defect found: `_validate_request_audit` (and the same pattern in `_preserve_verification_audit`) compares a `now()` string against the DB datetime, so every legitimate multi-save transition raises "Request audit fields cannot be changed." Plus three TASK-003 test-fixture mismatches, one test-assertion bug, and seven OCR pipeline failures traced to missing preloaded PaddleOCR models (`~/.paddlex` empty). No logic edited by the executor; site left in place; redis left running. Corrective phase 09b authored (`09b-task-002-003-corrective.txt`) and dispatched.
- 2026-07-22: Phase 09b completed at commit `cbea2159` (local, no push). Controller audit-datetime defect fixed via `_same_datetime` normalization (immutability re-verified — coordinator personally reviewed the diff); TASK-003 fixtures repaired through legitimate transitions; public-file rejection asserted; PaddleOCR 3.x engine-compat ported and models preloaded on the bench (`PP-OCRv6_medium_det/rec`, `PP-LCNet_x1_0_textline_ori`); synthetic fixture made OCR-legible. Suite improved to 55 pass / 0 errors / 6 failures. The 6 remaining failures are two genuine, exactly-diagnosed `ocr/image_preprocessor.py` defects: same-canvas rotation crops non-square images, and the CLAHE/median-blur chain degrades already-legible text. Corrective phase 09c authored (`09c-task-003-preprocessor-corrective.txt`) and dispatched.
- 2026-07-22: Phase 09c completed at commit `3486fccd` (local, no push). Defect A fixed with canvas-expanding rotation (`_rotate_expand`); Defect B fixed by emitting per-angle enhanced + raw-grayscale candidates (8 deterministic variants, PIL fallback identical). Unit test strengthened (candidate count 4→8, dimension-swap assertions). Full suite 61/61 pass on `visa-tracker-test.localhost`, pure tier 26/26; TASK-002 invariants re-verified inside the suite. Coordinator personally reviewed the preprocessor diff; an independent full-suite re-run was launched before accepting. passport_extractor runtime closure (TASK-002 + TASK-003) is complete — the coordinator's independent re-run also passed 61/61 in 222s; TASK-010 runtime phase for `the_visaguy`/`fileflo` and the E2E matrix is next.
- 2026-07-22: TASK-010 runtime phase executed on the reused dedicated site: `erpnext`, `crm`, `insights`, `processflo`, `fileflo` installed and migrated clean (worktree-first); `Lead`/`CRM Lead`/`Customer`/`PF Process File`/`FF File Collection` confirmed. `the_visaguy` migration FAILED on two genuine TASK-005 defects (executor stopped per contract, no edits): (A) the five tracking DocType folders sit at the Python-package top level `the_visaguy/doctype/` instead of the `The Visa Guy` module folder, so Frappe `sync_for` never discovers them — this also explains the long-standing "site-bound" test errors; (B) all six `visa_tracking_status.json` fixture records lack `name`/`modified`, aborting fixture sync with `KeyError: 'name'`. Coordinator independently verified the tree structure, module folder, fixture format, and the zero-`the_visaguy.doctype` import surface. Corrective phase 09d authored (`09d-task-005-corrective.txt`) and dispatched; TASK-010 runtime remainder (on-site suites, public-API/E2E runtime matrix) re-dispatch follows its acceptance. Evidence docs `10-task-010-planning.md`/`11-task-010-evidence.md` updated by the runtime executor.
- 2026-07-22: Phase 09d completed at commits `9f2393e6` + `28e58c04` (local, no push). Five tracking DocTypes moved into `the_visa_guy/doctype/` (pure renames + module `__init__.py`), fixture `name`/`modified` added, two test filesystem-path references fixed. Migration rc=0; SQL-verified: five DocTypes/tables, six active status records (`name = status_code`), 8 custom fields + 8 custom docperms converged idempotently, zero Guest rules. fileflo on-site 6/6. the_visaguy on-site (`--skip-test-records`): 216 ran / 201 pass / 12 skipped / 3 errors — the errors are two pre-existing genuine defects (test-isolation teardown destroying `frappe.local.db`; status uniqueness escaping as `DuplicateEntryError` instead of controller `ValidationError`) plus 12 stale "no dedicated test site" skips now ready to re-enable. Coordinator reviewed the 09d diff (renames, fixture keys, path fixes) and the TASK-005 spec. Corrective phase 09e authored (`09e-task-005-007-corrective.txt`) and dispatched.
- 2026-07-22: Phase 09e completed at commit `4593a845` (local, no push). Test-isolation teardown fixed (save/restore `frappe.local.db`); status uniqueness now raises friendly `frappe.ValidationError` (root cause: `autoname: field:status_code` pre-assigns `name` before `validate()`, excluding the colliding row from the self-exclusion filter); stale "no dedicated test site" guards removed. KEY FINDING: the 12 "integration test" classes were placeholder scaffolding — 27 methods whose bodies were only `skipTest("deferred to TASK-010")`; 09e kept honest per-method skips rather than manufacturing vacuous passes. On-site: the_visaguy 248 ran / 221 pass / 27 skip / 0 errors; fileflo 6/6; migrate rc=0; standalone baseline preserved. Coordinator enumerated all 27 placeholders across 12 files; they cover exactly the E2E/security matrix runtime items. Phase 09f authored (`09f-task-010-integration-tests.txt`) to implement the real bodies against the dedicated site, and dispatched.
- 2026-07-22: Takeover orchestrator (session resume) verified that phase 09f had NOT in fact executed — `the_visaguy` worktree was still at `4593a845` with a clean tree and no `09f-*.md` report existed. Re-verified all three remote worktree HEADs (`the_visaguy` `4593a845`, `fileflo` `fa1d6b38`, `passport_extractor` `3486fccd`, all clean) and the local frontend at `717c62d4`. Bench sites directory contains only `visaguy` and `visa-tracker-test.localhost`.
- 2026-07-22: Two executors dispatched in parallel on non-overlapping scopes. (1) Kimi runs phase 09f (backend integration tests on the dedicated remote site). Note: `kimi -p` rejects both `--auto` and `--yolo`; the working invocation is plain `kimi -p "$(cat <prompt>)" --add-dir <workspace>`. (2) A Claude Sonnet executor runs new phase 09g (`09g-frontend-e2e-harness.txt`) — frontend contract-conformance, full-journey E2E, privacy and a11y tests in local `visa_tracker`, fully offline against mocks with an explicit prohibition on touching the remote bench, so the two phases cannot collide. Both are instructed to write tests to the TASK-007 contract and STOP with evidence rather than edit application logic if behavior disagrees.
- 2026-07-22: NETWORK OUTAGE on the orchestrator host blocked phase 09f before any work began. Kimi read the workspace docs and planned its delegation, then died with `provider.connection_error` (`getaddrinfo EAI_AGAIN auth.kimi.com`). Independently characterized: `ping erpcode.tridz.in` = 100% packet loss, SSH port 2257 alternates "Network is unreachable"/"Connection timed out", `curl https://github.com` and `https://1.1.1.1` both return 000. Remote state confirmed UNCHANGED before the outage (`the_visaguy` `4593a845`, clean); the failed run wrote nothing to the bench and left no partial commits. Phase 09f is hard-blocked on connectivity, not on any code defect — re-dispatch verbatim with `kimi -p "$(cat prompts/09f-task-010-integration-tests.txt)" --add-dir <workspace>` once `ssh -p 2257 shahzad@erpcode.tridz.in` succeeds. Phase 09g (frontend, local-only, offline against mocks) is unaffected and continued running.
- 2026-07-22: Network restored (bench reachable after 10 polls; remote worktrees re-verified UNCHANGED at `4593a845`/`fa1d6b38`/`3486fccd`, clean). Phase 09f re-dispatched verbatim to Kimi.
- 2026-07-22: Phase 09g ACCEPTED at commit `a6a07a9a` in local `visa_tracker` (no push). Delivered a single typed wire-contract fixture module (`src/test/contract/wireContract.ts`) that every mock derives from so contract drift breaks in one place, contract-conformance tests, full-journey E2E through the real UI, privacy assertions, a11y gates, and an `npm run verify` script. Suite grew 16 → 41 tests; 41/41 twice, lint/tsc/build clean. COORDINATOR INDEPENDENT VERIFICATION (not taken on faith): (a) diff scope confirmed test-tree-only — the sole non-test file is `package.json`, and its diff is exactly the one-line `verify` script, so no application logic was touched; (b) re-ran the suite personally, 41/41; (c) ANTI-VACUITY MUTATION TEST — injected `localStorage.setItem('vt_token', token)` into `src/context/SessionContext.tsx` and the suite correctly went 3 failed / 38 passed across 3 files, proving the privacy tests genuinely fail when the memory-only-token guarantee is broken; mutation reverted, tree clean, 41/41 restored. No new defects found. The only limitation reconfirmed is the pre-existing TASK-007 one (no distinguishable mid-flow session-expiry signal; every failure returns the identical constant generic body) — already carried in `11-task-010-evidence.md` §8 item 4, now with direct runtime evidence, and NOT worked around in frontend logic. Still open for FEAT-001: backend runtime conformance (matrix items 8–11/16 backend leg) pending 09f, and live-backend browser smoke (matrix item 19).
- 2026-07-22: Phase 09f re-run STOPPED CORRECTLY with 0/27 bodies written, no commits, worktree clean at `4593a845` — it found two genuine application defects and refused to write tests around them (contract-compliant behavior). COORDINATOR INDEPENDENTLY CONFIRMED BOTH against live source and the live DB:
  - **D1 (severe): `visa_tracking/utils/settings.py` `_load_settings()` returns `None` on EVERY real site.** It guards on `frappe.db.table_exists("Visa Tracker Settings")`, but that DocType is a Single (`issingle: 1`) — Singles live in `tabSingles` and have no `tab<name>` table. Verified live: `SHOW TABLES LIKE 'tabVisa Tracker Settings'` returns nothing while `tabSingles` holds 33 rows for it. Blast radius: public verify/status/logout APIs always generic-fail as "disabled", `validate_cors` always fails closed, `create_tracking_application` always returns `None`, FileFlo inspection always disabled, `reconcile_missing_extractions` always `[]`. **The entire feature is non-functional at runtime.** Masked for the whole project because every pure-tier test mocks the settings loader — this is precisely the class of defect that only runtime testing can catch, and it justifies the on-site phases.
  - **D2: `services/extraction_orchestrator.py` `enqueue_passport_extraction` raises on every "created" extraction.** It does `job = frappe.enqueue(..., enqueue_after_commit=True)` then `job.id`; coordinator read installed Frappe v15 `utils/background_jobs.py` directly — the `enqueue_after_commit` branch does `frappe.db.after_commit.add(enqueue_call)` followed by a bare `return`, i.e. returns `None`, so `job.id` raises `AttributeError`.
  Corrective phase 09h authored (`09h-settings-loader-and-enqueue-corrective.txt`) and dispatched to a Claude Sonnet executor (stronger model chosen deliberately: the fixes are small but sit on security-sensitive paths — once D1 is fixed, CORS and the public APIs begin actually enforcing for the first time). Fix policies specified rather than left open: D1 must preserve the guard's legitimate pre-install intent via a DocType-existence check, not deletion; D2 must preserve after-commit safety via a self-generated `job_id`, explicitly NOT by dropping `enqueue_after_commit` (which would reintroduce the enqueue-before-commit race). 09h is scoped to D1/D2 only, must STOP and report any third defect, and must NOT write the 27 bodies. 09f re-dispatches to Kimi after 09h is accepted. Kimi also left useful resume facts in its transcript (PEX transitions are controller-enforced `Queued→Processing→Extracted→Verified`; `File` fixtures need a real image since a fake PDF fails `pypdf`; `TestSettings.test_predicates_default_to_false_when_missing` will conflict with the enabled Single baseline) and minor disposable litter on the test site (one synthetic PNG in `private/files/`, `/tmp/vt_probe_*.py`).
- 2026-07-22: Phase 09h ACCEPTED at commit `ccbd6324` in `the_visaguy` (local, no push). D1 fixed by replacing the `table_exists` guard with `frappe.db.exists("DocType", SETTINGS_DOCNAME)`, preserving the pre-install window and keeping the `DoesNotExistError` fallback. D2 fixed by generating a `uuid4().hex` job id, passing it as `job_id=` while KEEPING `enqueue_after_commit=True`, persisting that raw id to `last_job_id`, and returning the id string. COORDINATOR INDEPENDENT VERIFICATION (not taken on faith): (a) reviewed the full diff — 5 files, confined to D1/D2 plus their directly-invalidated tests, no unrelated app logic; (b) enumerated EVERY call site of `enqueue_passport_extraction` — exactly one production caller (`fileflo_inspection_service.py:91`), correctly updated from `job.id` to the returned id string (that caller was itself broken before); (c) verified the executor's job-id claim in installed Frappe source — `get_job()` calls `create_job_id()` internally (which prefixes `{site}::`), so the stored RAW id is genuinely usable with `is_job_enqueued`/`get_job`; the claim is accurate; (d) RE-PROVED D1 AT RUNTIME MYSELF on `visa-tracker-test.localhost`, output showing defect and fix side by side: old guard `table_exists("Visa Tracker Settings")` = False (confirming the defect was real), new guard `db.exists("DocType", ...)` = True, `get_visa_tracker_settings()` returns the real Single with `enabled: 1`, and `is_tracking_enabled()` = True (previously always False). Suites at `ccbd6324`: on-site 248 ran / 221 pass / 27 skip / 0 errors (identical across two runs — expected to be unchanged since pure-tier tests mock the loader), fileflo 6/6, standalone 199/187/10 skip/2 pre-existing errors. No third defect surfaced. Coordinator removed its own probe script and the earlier `/tmp/vt_probe_*.py` litter from the bench.
- 2026-07-22: Phase 09f re-dispatched to Kimi against the new baseline. The 09f prompt was UPDATED before dispatch (do not re-send the old one): new HEAD `ccbd6324`, an explicit "these two are already fixed, do not re-report them, and note `enqueue_passport_extraction` now returns a STRING not a Job" section, the confirmed test-site prerequisites, the fixture facts recovered from the blocked attempt (controller-enforced PEX transitions; `File` fixtures need a real image because a fake PDF fails `pypdf`; PF Process File lacks the custom reference fields; whatsapp handlers early-return), and the exact `ccbd6324` suite baselines. IMPORTANT EXPECTATION for whoever reviews 09f: D1 meant large parts of this feature had never executed on a real site, so the 27 real integration bodies are the first genuine runtime exercise of the public API, CORS, session, rate-limit and audit paths — further genuine defects surfacing there is the LIKELY outcome, not a sign the phase failed.
- 2026-07-22: Phase 09f ACCEPTED at commit `088430f4` in `the_visaguy` (local, no push): 22 of 27 integration bodies implemented and passing. On-site 248 ran / 243 pass / 5 skipped / 0 errors (identical across the executor's two runs AND its own independent third re-run); fileflo 6/6; standalone 199 ran / 187 pass / 12 skipped / 0 errors. E2E matrix items 1, 3, 6-11, 15, 17 runtime-closed, 16 at payload level. The 5 still-skipping bodies are individually justified by two NEW genuine application defects, which the executor correctly refused to write tests around. This confirms the expectation recorded before dispatch: the first real runtime exercise of these paths surfaces real defects.
  COORDINATOR INDEPENDENTLY CONFIRMED BOTH:
  - **D3 (severe, data-model): unbraced `format:` autonames mean only ONE row can ever exist per DocType per site.** `visa_tracking_application.json`, `visa_tracking_status_log.json` and `visa_tracker_audit_log.json` declare e.g. `"autoname": "format:VTA-.YYYY.-.#####"`. Frappe's `frappe/model/naming.py::_format_autoname` substitutes only BRACED params — confirmed from its own docstring ("replacing all instances of braced params", example `format:LOG-{MM}-{fieldname1}-{#####}`) and its use of `BRACED_PARAMS_PATTERN`. The dotted form is naming-series syntax used WITHOUT the `format:` prefix. As written nothing substitutes, every record is named the literal string, and the second insert raises `DuplicateEntryError` (1062). Aggravating: the audit path swallows this via `_safe_insert_event`, so audit rows are silently dropped rather than erroring.
  - **D4 (severe): `NameError` breaks the public status endpoint for every real application.** `visa_tracking/services/status_service.py:254` inside `get_public_display_timeline` does `_iso_or_empty(effective_on)` while the loop variable is `row`. Coordinator proved by AST analysis that the function neither takes nor binds `effective_on` (the bindings at lines 81/154 are in other functions), so any application with >=1 log row raises `NameError` and the public status endpoint degrades to generic failure.
- 2026-07-22: Corrective phase 09i authored (`09i-autoname-and-timeline-corrective.txt`) and dispatched to a Claude Sonnet executor (stronger model again: D3 is a naming/data-model change and D4 sits on the public endpoint). Fix policies specified rather than left open: D3 must pick ONE convention (braced `format:VTA-{YYYY}-{#####}` OR dotted with the `format:` prefix REMOVED) and apply it consistently to all three DocTypes, proving at runtime that >=3 consecutive inserts yield distinct incrementing names; D4 is a one-line fix to `row.get("effective_on")`. 09i also implements the final 5 bodies (fixtures from 09f are already in place) and must STOP on any third defect. NOTE for closure: the feature has never been deployed and `visaguy` does not have these DocTypes, so D3 needs no production data migration — but if that ever changes, an autoname change on populated tables would require a rename migration.
- 2026-07-22: Phase 09i ACCEPTED at commit `910914e` in `the_visaguy` (local, no push). D3 fixed by applying braced format consistently to all three DocTypes (`format:VTA-{YYYY}-{#####}`, `VTL-`, `VTAL-`); D4 fixed by the one-line `row.get("effective_on")`. All 27/27 integration bodies now implemented and passing. COORDINATOR INDEPENDENT VERIFICATION (not taken on faith): (a) reviewed the full diff — the only application changes are the three autoname strings and the single `status_service.py:254` line; everything else is test code, no scope creep; (b) RAN THE FULL ON-SITE SUITE MYSELF: `248 ran ... OK`, zero skips, zero errors, a third independent green run; (c) PROVED D3 AT RUNTIME MYSELF with a side-by-side probe — the NEW braced pattern generated three distinct incrementing names (`VTAL-2026-00013/00014/00015`, distinct=True) while the OLD pattern generated the identical literal string `VTAL-.YYYY.-.#####` twice (collides=True), confirming both that the defect was real and that the fix resolves it. Suites: on-site 248/248 (0 skip, 0 error, identical across runs), fileflo 6/6, standalone 199/187/12 skip/0 errors unchanged. No third defect surfaced.
- 2026-07-22: STATUS OF FEAT-001 RUNTIME CLOSURE. Green and coordinator-verified: passport_extractor 61/61; the_visaguy on-site 248/248 with all 27 real integration bodies; fileflo 6/6; frontend 41/41 with mutation-tested privacy assertions. Four genuine defects that the 197-test static tier had missed were found and fixed only because of runtime execution (settings-loader Single guard, enqueue job-id, autoname collision, public-timeline NameError) — two of which made the feature non-functional. REMAINING before TASK-010 can be closed: (1) E2E matrix item 19, live-backend browser smoke — the frontend has never been exercised against the real backend, only against MSW mocks, and given this feature's history that gap is the highest-value remaining check; it needs an SSH tunnel plus Host-header/CORS handling for `visa-tracker-test.localhost`. (2) Workspace closure by the maintainer: move TASK-002 and TASK-010 from `tasks/in-progress/visa-tracking/` to completed and set FEAT-001 `status: completed` — ONLY after (1). Item 18 (human visual-parity) was already accepted provisionally by the project owner with polish deferred. NOTHING HAS BEEN PUSHED in any repository; push/deploy remains unauthorized and is the user's decision.
- 2026-07-22: Phase 09j COMPLETED. Live HTTP smoke of the three Guest endpoints against site `visa-tracker-test.localhost` at `910914e`, compared field-by-field with the frontend's `wireContract.ts` at `a6a07a9a`. Driven over REAL HTTP (loopback werkzeug on 127.0.0.1:9711 + curl with an explicit Host header), never in-process calls.
  PROCESS NOTE / CORRECTION: the Kimi executor appeared stalled (no stdout flush for ~41 min, port never listening) and the coordinator terminated it and re-ran the smoke directly. That read was WRONG — Kimi had in fact completed the full L1-L9 probe set and written a thorough 28KB report before hanging at the very end. Its report is retained and is the primary record; its session-expiry probe (manipulating a real Redis TTL, then proving a fresh token still worked) was more rigorous than the coordinator's. Lesson for future phases: check for the phase's OUTPUT FILE before judging an executor idle from its stdout log alone. A second wrong read was also corrected during this phase: the ~10 `bench serve` processes on the host are NOT ours — this is a SHARED server and they belong to other users (swafa, shahala, ameen, jezlan, vaishna, sanjusha, fathima, vyshnav). Checking `ps` ownership before killing avoided disrupting a colleague's dev server. Do not kill processes on this host without verifying the owner is `shahzad`.
  RESULT — CONTRACT: ZERO mismatches. The live envelope is `{success, message, data}` at top level (not Frappe's `{"message": ...}` wrapper), exactly as the frontend expects. All 9 `STATUS_RESPONSE_KEYS` present with no extras/omissions; timeline items exactly `{status, message, effective_on, icon}`; no `FORBIDDEN_STATUS_RESPONSE_FIELDS` in any body; generic-failure body byte-identical across every failure mode. POST-only enforced (GET → PermissionError); Guest `/api/resource/Visa Tracking Application` denied; CORS omits `Access-Control-Allow-Origin` for a disallowed origin and echoes it exactly for an allowed one. Incidentally re-confirmed outside the suite: autonames increment in real use (`VTA-2026-00041`, `VTL-2026-00042` — D3 holds) and `effective_on` is populated on the real data path (D4 holds). The frontend's MSW mocks are a faithful model of the real backend.
  RESULT — NEW SECURITY FINDING (medium-high, NOT fixed, recorded as Appendix C of the 09j report): rate limiting does not constrain credential enumeration. `verification.py:102` sets `target_bucket = candidates[0][0]` (the passport+DOB lookup hash), so `build_client_key(origin_scope, source_ip, target_bucket)` scopes the failure counter per exact credential pair. Coordinator proved empirically: 7x the IDENTICAL wrong pair locks out at attempt 6 with `Retry-After: 900` (works as designed), but 7x the same passport with an incrementing DOB NEVER locks out (0/7). An attacker who knows a passport number can enumerate DOBs (~36,500 candidates for an adult) from one IP unthrottled; enumerating passport numbers against a fixed DOB is equally unconstrained. The constant-200 design means there is no client-visible signal that throttling is absent, which is why both the 248-test in-process suite and the executor's own passing L9 missed it. Recommended (needs a TASK-007 amendment + product sign-off, deliberately NOT applied): add a complementary coarser per-IP/per-origin counter with a higher threshold alongside the existing per-identity one, preserving the anti-lockout-DoS property while closing enumeration.
  REMAINING before FEAT-001 closure: (1) the rate-limit finding needs a product decision; (2) E2E matrix item 19 BROWSER leg — wire conformance is proven but no real browser has rendered against the live backend; (3) item 18 human visual-parity already accepted provisionally with polish deferred; (4) workspace-maintainer closure (move TASK-002/TASK-010 to completed, set FEAT-001 `status: completed`) which should NOT happen while (1) is open. NOTHING PUSHED in any repository.
